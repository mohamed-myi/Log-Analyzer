# Log Analyzer

A C++ utility for parsing, filtering, and analyzing log files. Supports filtering by log level, date range, and source component with statistics generation.

## Setup

### Requirements
- CMake 3.10+
- C++17 compiler (g++, clang, or MSVC)

### Build